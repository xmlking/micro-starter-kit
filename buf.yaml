build:
  roots:
    - srv
    - shared
    - third_party/proto

lint:
  use:
    - DEFAULT
    - FILE_LOWER_SNAKE_CASE
  except:
    # - PACKAGE_AFFINITY
  ignore:
    - protoc-gen-gorm
    - validate
  service_suffix: Service

breaking:
  use:
    - WIRE_JSON
