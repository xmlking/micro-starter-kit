apiVersion: apps/v1
kind: Deployment
metadata:
  name: srv
spec:
  template:
    spec:
      # TODO: this work. shouldn't hard-code serviceAccountName
      # serviceAccountName: $(CONSUL_SA)
      serviceAccountName: k8s-micro-v1
